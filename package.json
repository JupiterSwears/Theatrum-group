{
  "name": "landingpage",
  "version": "1.0.0",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "NODE_OPTIONS=--dns-result-order=ipv4first tinacms dev -c \"hugo server -D --bind=0.0.0.0\"",
    "build": "NODE_OPTIONS=--dns-result-order=ipv4first tinacms build && hugo --minify"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "description": "",
  "devDependencies": {
    "@tinacms/cli": "^2.3.1",
    "tinacms": "^3.8.1"
  }
}
